// TODO: Steps 3+ — PerspectiveCamera, OrbitControls, lights, render loop
export function useThreeScene() {
  function init(_container: HTMLElement): void {
    // stub
  }

  function dispose(): void {
    // stub
  }

  return { init, dispose }
}
