# CLAUDE.md

## Rules

- Do NOT run `bun run build` in any directory: dev environment, hot reload.
- Never read files inside `node_modules/`.
- Ignore tests unless explicitly asked to run or modify.
- Use Windows PowerShell.
