import type { ColorGroup, KeychainConfig } from '../types/keychain'

// TODO: Steps 3–5 — build base plate + logo extrusion + keyring hole geometries
export function useKeychainBuilder() {
  function build(_colorGroups: ColorGroup[], _config: KeychainConfig): void {
    // stub
  }

  return { build }
}
